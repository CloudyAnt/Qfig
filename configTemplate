# This is a config example, cp this as 'config'

## Add needed commands in this label. Available commands are under commands folder
<enabledCommands>
docker
curl
</enabledCommands>


## Set prefer text editor. It can be a command-line editor(vim, nano) or a command-line shortcut of a editor with UI(NotePad, code).
## Note: code is the command-line shortcut for VSCode
<preferTextEditor>code</preferTextEditor>